version: '3.8'

services:
  # define o serviço da API.
  api:
    # constrói a imagem Docker a partir do Dockerfile no diretório atual.
    build: .
    container_name: simulador_api
    ports:
      - "8080:8080"
